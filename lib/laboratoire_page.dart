import 'package:flutter/material.dart';

class LaboratoirePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page du Laboratoire'),
      ),
      body: Center(
        child: Text('Contenu de la page du Laboratoire'),
      ),
    );
  }
}
